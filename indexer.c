#include "map.h"
#include "tokenizer.h"

/*
void consume(hash_map term_map, char* const content, char* const  file_name, int* const  line) {
    TokenStack ts = {
        .top = NULL,
        .current_line = 1    
    };
    
    while (read_next_token(ts, &content);

    Token* token;
    while (token = pop_token(ts)) {
        hash_map file_set = hash_map_get(term_map, token);
        if(file_set == NULL) {
            
            hash_map_put(map,  
        }
        
    }
}
*/
